# 4. Dadas a base e a altura de um retângulo, qual sua área? A=base⋅altura

base = float(input("Digite a base: "))
altura = float(input("Digite a altura: "))

A = base * altura

print("A área é igual a ", A)